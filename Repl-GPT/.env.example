# Public domain users access in their browser (must match for Solana signature verification)
# Must match deployed hostname (e.g. your-repl.replit.app) for wallet auth to work
# If unset, falls back to REPL_SLUG.REPL_OWNER.repl.co or localhost
PUBLIC_APP_DOMAIN=

DATABASE_URL=
# JWT_SECRET - unused (not referenced in server; kept for compatibility)
JWT_SECRET=

# Required in production; long random string for first admin creation
BOOTSTRAP_ADMIN_KEY=
SOLANA_RPC_URL=
HIVE_MINT=
HIVE_VAULT_ADDRESS=
OLLAMA_API_KEY=
OLLAMA_BASE_URL=https://ollama.replit.dev
OLLAMA_MODEL=gemma3:12b
OLLAMA_EMBED_MODEL=nomic-embed-text
# Must match your embedding model output dimension AND DB vector column dimension
OLLAMA_EMBED_DIM=1024
CREATOR_WALLETS=
MIN_HIVE_ACCESS=50

# Sentry (optional): VITE_SENTRY_DSN=client, SENTRY_DSN=server (ignore VITE_* in server validation)
VITE_SENTRY_DSN=
SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
MIN_USD_ACCESS=1
